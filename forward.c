// forward.c: Implements the 'forward pass' of a neural network, passing input
// through the network to produce some output class(es). This provides everything
// needed for inference in straight C.

#include <stdio.h>

int main(int argc, char **argv) {
    printf("Hello, world!\n");
    return 0;
}

